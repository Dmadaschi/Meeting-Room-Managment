<!DOCTYPE html>
<html>
  <head>
    <title>MeetingRoomManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if user_signed_in? %>
        <h4>Reserva de Sala</h4>
        <h4><%="Usúario: #{current_user.username}"%></h4>
        <h4><%= link_to "Sair", destroy_user_session_path, method: :delete %></h4>
      <% end %> 
    </nav>
    <%= yield %>
  </body>
</html>
